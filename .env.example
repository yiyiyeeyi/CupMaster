# Public project URL and browser-safe publishable key. RLS remains the security boundary.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Used only by explicit CLI commands such as type generation; never shipped to the browser.
SUPABASE_PROJECT_REF=
