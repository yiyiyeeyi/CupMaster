import{describe,expect,it}from"vitest";import{migrateSuggestedPlanCandidate,LOCAL_SUGGESTED_PLAN_VERSION}from"./local-suggested-plan-schema";
describe("Suggested Plan v2 migration",()=>{it("uses version 2",()=>expect(LOCAL_SUGGESTED_PLAN_VERSION).toBe(2));it("normalizes used without timestamp and preserves mapping",()=>expect(migrateSuggestedPlanCandidate({id:"p",status:"used",resultingBrewId:"b",usedAt:null})).toMatchObject({candidate:{status:"draft",resultingBrewId:"b",usedAt:null},warnings:[{planId:"p",code:"legacy_used_without_timestamp"}]}));it("records used without Brew instead of dropping it",()=>expect(migrateSuggestedPlanCandidate({id:"p",status:"used",resultingBrewId:null,usedAt:"2026-01-01T00:00:00.000Z"})).toMatchObject({candidate:{status:"draft",resultingBrewId:null,usedAt:null},warnings:[{planId:"p",code:"legacy_used_without_resulting_brew"}]}));it("leaves valid draft mapping unchanged",()=>expect(migrateSuggestedPlanCandidate({id:"p",status:"draft",resultingBrewId:"b",usedAt:null}).warnings).toEqual([]))});
